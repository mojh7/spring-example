/*
package com.example.geo;

import com.example.geo.ref.config.ExternalApiConfig;
import lombok.AllArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RestController;

@Slf4j
@RestController
@AllArgsConstructor
public class TempController {
    private final ExternalApiConfig externalApiConfig;

    @PostMapping("/api/members/temp")
    public String temp() {
        */
/*log.info(String.valueOf(externalApiConfig.getSnsApiDomains().size()));
        for(Map.Entry<String, String> entry : externalApiConfig.getSnsApiDomains().entrySet()) {
            System.out.println(String.format("%s : %s", entry.getKey(), entry.getValue()));
        }*//*

        return "test";
    }
}
*/
